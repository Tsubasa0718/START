<div class="table-body">
    <table class="table">
        <% tableData.forEach(item => { %>
            <tr class="table__tr">
                <th class="table__th"><%= item.head %></th>
                <td class="table__td"><%= item.data %></td>
            </tr>
        <% }); %>
    </table>
</div>